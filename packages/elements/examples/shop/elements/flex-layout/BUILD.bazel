package(default_visibility = ["//visibility:public"])

load("//tools:defaults.bzl", "ng_module", "ts_library")

ng_module(
    name = "flex-layout",
    srcs = glob(["*.ts"]),
    tags = ["ivy-only"],
    deps = [
        "//packages/elements/platform",
    ],
)
